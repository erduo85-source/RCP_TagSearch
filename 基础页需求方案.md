# 多维画像查询基础页 - 需求方案

## 1. 结论

基础页先只承载「查询入口」职责：让风控运营/分析人员能选择账号、设备、IP 三种维度，输入查询对象，并通过最近查询快速复用历史对象。查询结果页、画像详情、风险证据等内容暂不在本阶段展开，查询后先打开弹窗，弹窗内容后续再确认。

本阶段目标是根据 Figma 已有设计稿实现像素级保真的基础页原型，确保可以用于分享和原型演示，并支持上传到 Git 仓库后通过 Pages 访问。

## 2. 范围确认

| 项目 | 决策 |
| --- | --- |
| 页面名称 | 多维画像查询 |
| 页面定位 | 用户画像中心下的查询入口页 |
| 主要用户 | 风控运营、风控分析、业务排查人员 |
| 使用场景 | 已知账号、设备号或 IP，希望快速发起画像查询 |
| 第一阶段输出 | 可分享、可演示、可通过 Git Pages 托管的高保真基础页原型 |
| 查询结果 | 本阶段不设计详情内容；点击查询后展示弹窗，弹窗内容稍后确认 |
| 数据来源 | 最近查询写死 mock 数据，仅用于评审展示，不接真实接口 |
| 技术复杂度 | 原型优先，不引入后端、数据库、登录、权限、复杂部署 |
| 适配范围 | 只做桌面端后台页面，不需要移动端适配 |

## 3. 页面结构

| 区域 | 模块 | 需求说明 |
| --- | --- | --- |
| 顶部导航 | 平台安全中心 Header | 展示系统品牌、顶部菜单、搜索框、消息/通知/用户入口；按 Figma 还原即可，暂不做真实功能 |
| 左侧导航 | 用户画像中心菜单 | 展示「支付风控引擎」「用户风控引擎」「用户画像中心」分组，并高亮「多维画像查询」 |
| 内容页头 | 标题与说明 | 标题为「多维画像查询」；说明为「支持账号、设备、IP维度的用户洞察，快速定位登录、注册、支付风险」 |
| 查询卡片 | 查询维度 Tab | 支持「按账号」「按设备」「按IP」三类入口，默认选中「按账号」 |
| 查询卡片 | 字段选择 | 根据当前维度展示字段下拉，如账号名、设备号、IP |
| 查询卡片 | 查询输入 | 输入框 placeholder 随维度变化 |
| 查询卡片 | 立即查询 | 点击后触发基础校验，并进入后续查询结果流程 |
| 查询卡片 | 辅助说明 | 根据维度说明支持的检索对象 |
| 最近查询 | 历史标签 | 展示历史查询对象，支持点击回填 |
| 最近查询 | 清空记录 | 清空全部历史记录，空状态时隐藏或置灰 |

## 4. 查询维度规则

| 维度 | 字段选项 | 输入提示 | 辅助说明 |
| --- | --- | --- | --- |
| 按账号 | 账号名、通行证ID、登录手机、SDKID | 请输入账号名或SDKID | 支持通过账号名、通行证ID、登录手机、SDKID，快速检索风险账号 |
| 按设备 | 设备ID、设备指纹、IMEI/IDFA | 请输入设备ID或设备指纹 | 支持通过设备标识，快速检索关联账号与风险设备 |
| 按IP | IP地址 | 请输入IP地址 | 支持通过IP地址，快速检索风险账号 |

说明：字段选项用于原型表达业务口径，第一阶段不做复杂格式识别。若输入为空，点击查询时给出轻量提示，不进入结果页。

## 5. 基础交互

| 交互 | 规则 |
| --- | --- |
| 切换 Tab | 当前维度高亮；字段选项、placeholder、辅助说明同步变化；保留输入框内容 |
| 字段下拉 | 当前阶段只影响输入提示和后续查询参数，不展开高级筛选 |
| 输入内容 | 支持普通文本输入；去除首尾空格 |
| 点击立即查询 | 空输入提示；非空则打开查询结果弹窗，弹窗内容后续确认 |
| 点击最近查询标签 | 自动切换到对应维度，回填字段和值，可再次点击立即查询 |
| 删除单条历史 | 为贴合 Figma 样式可展示关闭图标，但第一阶段不要求真实删除 |
| 清空记录 | 展示「清空记录」入口即可，第一阶段不要求真实清空 |

## 6. 页面状态

| 状态 | 展示要求 |
| --- | --- |
| 默认态 | 按账号 Tab 选中；输入框为空；最近查询有 mock 标签 |
| 维度切换态 | Tab、字段、placeholder、辅助说明更新，输入内容保留，无布局跳动 |
| 输入态 | 输入框内容清晰可读，长文本不撑破容器 |
| 校验态 | 空输入时提示「请输入查询内容」或同等短提示 |
| 提交态 | 按钮进入短暂 loading；随后打开弹窗 |
| 弹窗态 | 打开查询结果承接弹窗；标题、查询对象、关闭按钮可先固定，正文稍后确认 |
| 历史空态 | 不作为第一阶段必做状态，最近查询使用固定 mock 数据 |
| 桌面态 | 以 Figma 桌面画布为准，不做移动端适配 |

## 7. 最近查询规则

| 规则 | 说明 |
| --- | --- |
| 初始数据 | 使用 Figma 中已有样例，如 `10001_12392193311`、`DEVB-1311`、`DEV-54C291`、`192.21.11.8`、`10.21.66.14`、`user_8825`、`SDK08521` |
| 新增记录 | 不新增，写死 mock 数据即可 |
| 去重规则 | 不处理，写死 mock 数据即可 |
| 数量上限 | 按 Figma 视觉效果展示两行左右标签即可 |
| 标签样式 | 保持 Figma 的浅灰胶囊标签样式，带类型图标和关闭入口 |

## 8. 查询结果衔接

| 场景 | 本阶段处理 |
| --- | --- |
| 点击立即查询且输入有效 | 打开查询结果弹窗 |
| 后续结果页方案未确认 | 不在基础页塞入画像详情、风险图谱、事件列表 |
| 弹窗内容未确认 | 先展示弹窗框架、查询对象和关闭动作，正文区域留待后续确认 |
| 结果页方案确认后 | 基础页只负责传递 `queryType`、`field`、`keyword` 三类参数给弹窗或后续结果页 |

推荐参数结构：

```json
{
  "queryType": "account",
  "field": "accountName",
  "keyword": "10001_12392193311"
}
```

## 9. 像素级还原要点

| 对象 | 还原要求 |
| --- | --- |
| 画布 | 按 Figma 桌面端 1920px 宽画面优先还原 |
| Header | 深蓝背景，高度约 60px，元素位置和字号贴近设计稿 |
| Sidebar | 宽度约 200px，白底，当前菜单浅蓝高亮 |
| 内容背景 | 浅灰背景，卡片为白底 |
| 页头 | 标题蓝色，说明文字灰色，信息层级与 Figma 一致 |
| 查询卡片 | 圆角、阴影、内边距、Tab 下划线、输入框高度、按钮蓝色都按设计稿 |
| 最近查询 | 标题、清空记录、标签间距、换行节奏按设计稿 |
| 查询弹窗 | 先保证弹窗居中、遮罩、标题、关闭动作符合后台产品质感，具体正文后续补充 |
| 字体 | 中文优先使用系统中文字体或 Noto Sans SC，避免异常字重 |

## 10. 验收标准

| 验收项 | 通过标准 |
| --- | --- |
| 视觉还原 | 与 Figma 基础页首屏在模块顺序、颜色、间距、字号、圆角上高度一致 |
| 维度切换 | 按账号、按设备、按 IP 三个入口可切换，内容同步变化且保留输入 |
| 查询校验 | 空输入不进入下一步，有明确提示 |
| 查询弹窗 | 有效输入后打开弹窗，可关闭；弹窗正文后续确认 |
| 历史记录 | 固定 mock 标签展示，可点击回填；删除和清空不作为第一阶段强交互 |
| 布局稳定 | 标签过多、输入较长、页面缩放时无遮挡和异常换行 |
| 原型闭环 | 可从进入页面到输入查询，再到弹窗承接 |
| 分享演示 | 静态前端工程可打包，可上传 Git 仓库并通过 Pages 演示 |
| 验证方式 | 本地运行后做浏览器级截图检查，对照 Figma 主画面 |

## 11. 待确认问题

| 问题 | 已确认决策 |
| --- | --- |
| 技术形态 | 确保可分享和原型演示，可上传 Git 仓库生成 Pages；实现上优先选择静态前端友好的方案 |
| 查询后跳转方式 | 不跳转，点击查询后展示弹窗 |
| 切换维度是否保留输入 | 保留输入 |
| 最近查询是否本地持久化 | 不持久化，写死 mock 数据即可 |
| 是否需要移动端适配 | 不需要移动端适配，只做桌面端 |
