# 风控系统-多维画像查询

**文档变更记录：**

| 版本号 | 更新时间 | 更新内容 | 更新人 |
| --- | --- | --- | --- |
| V1.0 | 2026-07-27 | 新增多维画像查询基础页需求，明确查询入口、字段选择、最近查询、查询弹窗和检查要点 | Codex |
| V1.1 | 2026-07-27 | 调整文档为开发、测试可执行口径，补充最近查询删除与清空规则 | Codex |
| V1.2 | 2026-07-27 | 移除正文中的样例数据口径，明确最近查询按用户历史记录展示 | Codex |
| V1.3 | 2026-07-27 | 补充账号查询结果弹窗结构、日志范围、标签展开、复制、导出、空状态和异常提示规则 | Codex |
| V1.4 | 2026-07-27 | 补充设备/IP查询结果弹窗、字段占位符动态变化、空输入报错、分页条数切换和三维度关联信息规则 | Codex |
| V1.5 | 2026-07-27 | 按 product-prototype-workflow 的 PRD 标准结构重排文档，仅保留需求背景、需求描述、附录三个顶层结构 | Codex |
| V1.6 | 2026-07-27 | 收敛正文标题层级至三级以内，删除非需求内容 | Codex |

## 一、需求背景

:::
原有问题：风控运营、风控分析人员在排查用户风险时，需要分别从账号、设备、IP 等不同对象切入，查看对象基础信息、风险标签、近 30 日日志和关联对象。当前缺少统一查询入口和统一结果承载页面，排查链路分散，影响风险定位效率。

本期能力：建设“多维画像查询”页面，支持按账号、按设备、按 IP 三类维度发起查询；查询成功后在当前页面打开画像结果弹窗，展示基础信息、标签信息、日志信息、关联信息，并支持字段复制、日志分页、导出详情和空状态展示。
:::

## 二、需求描述

:::
原型链接：[本地原型](http://127.0.0.1:4173/)
设计稿链接：[风控系统 Figma 设计稿](https://www.figma.com/design/9JqT9uhKrNIwOgnYZQL1ox/%E9%A3%8E%E6%8E%A7%E7%B3%BB%E7%BB%9F?node-id=2470-6307)
:::

### 1. 用户流程

1. 风控运营或风控分析人员进入“平台服务中心 - 风险管理 - 用户画像中心 - 多维画像查询”页面，页面默认停留在“按账号”维度。
2. 用户在查询卡片中选择查询维度，可在“按账号 / 按设备 / 按IP”之间切换。
3. 系统根据当前查询维度展示字段下拉选项、输入框占位符和辅助说明。切换维度时，各维度已输入内容保留。
4. 用户选择查询字段并输入查询内容后，点击“立即查询”。
5. 用户点击“立即查询”时：
   - 若查询内容为空，系统阻断查询，不打开弹窗，并展示错误提示。
   - 若查询内容不为空，系统打开对应维度的画像查询结果弹窗。
6. 用户可点击最近查询标签，系统自动切换到标签对应的查询维度，并回填查询字段和查询内容。
7. 用户在结果弹窗内查看基础信息、标签信息、日志信息和关联信息。
8. 用户可在结果弹窗内完成以下操作：
   - 展开或收起基础信息。
   - 切换日志 tab。
   - 切换日志分页条数。
   - 复制标识类字段。
   - 导出当前查询对象的日志详情。
   - 点击右上角关闭按钮或按 Esc 关闭弹窗。
9. 用户关闭弹窗后返回查询页，查询页维度、字段和输入内容保持当前状态。

---

**流程判断逻辑：**

| 判断项 | 逻辑 |
| --- | --- |
| 切换查询维度是否清空输入 | 不清空，各维度输入内容分别保留 |
| 查询字段是否随维度变化 | 是，账号、设备、IP 分别展示对应字段 |
| 输入框占位符是否随字段变化 | 是，占位符根据当前下拉字段动态变化 |
| 输入为空是否允许查询 | 不允许，阻断查询并提示“请输入{当前字段名}” |
| 查询成功后是否跳转页面 | 不跳转，在当前页面打开结果弹窗 |
| 账号维度日志范围 | 仅展示登录、支付 |
| 设备/IP维度日志范围 | 展示登录、注册、支付 |
| 日志默认时间范围 | 固定近 30 日，不提供时间筛选 |
| 日志默认分页 | 默认 5 条/页，支持 10 条/页、20 条/页 |
| 关联对象是否可点击跳转 | 不支持跳转，仅支持复制 ID 后手动查询 |
| 标签是否支持删除 | 不支持删除，不展示删除图标 |
| 弹窗底部按钮 | 本期不展示确认、取消等底部按钮 |
| 移动端适配 | 不要求，仅面向桌面后台页面 |

### 2. 页面说明

**2.1 多维画像查询页**

页面用于承载画像查询入口和最近查询记录。

**页面相关内容：**

| 字段/模块 | 展示说明 |
| --- | --- |
| 顶部导航 | 展示平台品牌、顶部菜单、搜索入口、工具图标、当前用户 |
| 左侧导航 | 展示风险相关菜单，并高亮“多维画像查询” |
| 页面标题 | 展示“多维画像查询” |
| 页面说明 | 展示“支持账号、设备、IP维度的用户洞察，快速定位登录、注册、支付风险” |
| 查询卡片 | 包含维度 tab、字段下拉、输入框、查询按钮、辅助说明 |
| 最近查询卡片 | 展示当前用户最近查询记录，默认最多 10 条 |

**查询字段：**

| 查询维度 | 字段选项 | 默认字段 | 占位符规则 | 辅助说明 |
| --- | --- | --- | --- | --- |
| 按账号 | 账号名、通行证ID、登录手机、SDKID | 账号名 | 请输入{字段名} | 支持通过账号名、通行证ID、登录手机、SDKID，快速检索风险账号 |
| 按设备 | 设备ID、设备指纹 | 设备ID | 请输入{字段名} | 支持通过设备ID、设备指纹，快速检索风险账号 |
| 按IP | IP | IP | 请输入IP地址 | 支持通过IP地址，快速检索风险账号 |

**状态及交互：**

| 场景 | 说明 |
| --- | --- |
| 默认状态 | 默认选中“按账号”，字段为“账号名”，输入框为空 |
| 切换维度 | tab 高亮切换，字段下拉、占位符、辅助说明同步变化 |
| 选择字段 | 输入框占位符立即更新 |
| 输入保留 | 各维度分别保留最近一次输入内容 |
| 空输入查询 | 阻断查询；输入框下方和 toast 提示“请输入{当前字段名}” |
| 查询失败 | toast 提示“查询失败，请稍后重试” |
| 查询成功 | 打开对应画像查询结果弹窗 |

**2.2 最近查询**

最近查询用于帮助用户快速复用历史查询对象。

**模块内容：**

| 模块 | 功能说明 |
| --- | --- |
| 标题 | 展示“最近查询” |
| 查询标签 | 展示当前用户最近查询记录 |
| 标签图标 | 根据对象类型展示账号、设备、IP 对应图标 |
| 清空记录 | 清空当前用户全部最近查询记录 |

**状态及交互：**

| 场景 | 说明 |
| --- | --- |
| 默认展示 | 默认最多展示 10 条，按最近查询时间倒序 |
| 点击标签 | 自动切换到对应查询维度，并回填字段和查询内容 |
| 新增记录 | 查询成功后写入最近查询记录 |
| 重复记录 | 同一用户、同一维度、同一字段、同一内容视为重复记录；重复查询时更新时间并置顶 |
| 数量上限 | 最多保留 10 条，超出后移除最早记录 |
| 删除单条 | 删除当前标签对应记录，并从列表移除 |
| 清空记录 | 清空当前用户全部最近查询记录 |
| 空状态 | 无记录时展示“暂无最近查询” |

**2.3 画像查询结果弹窗**

点击“立即查询”且查询成功后，打开当前查询维度对应的结果弹窗。弹窗覆盖在查询页上方，背景页面置灰；弹窗内容超过视口高度时，弹窗内部滚动。

**弹窗包含：**

| 模块 | 功能说明 |
| --- | --- |
| 弹窗标题 | 根据查询维度展示“账号画像查询 / 设备画像查询 / IP画像查询” |
| 基础信息 | 展示当前查询对象的基础属性，支持展开/收起 |
| 标签信息 | 展示当前查询对象命中的标签 |
| 日志信息 | 展示当前查询对象近 30 日相关日志 |
| 关联信息 | 展示与当前对象关联的其他对象 |
| 关闭入口 | 支持右上角关闭和 Esc 关闭 |
| 导出详情 | 导出当前查询对象所有日志 tab 数据 |

**权限说明：**

| 权限项 | 说明 |
| --- | --- |
| 页面访问权限 | 有用户画像中心查看权限的用户可访问 |
| 查询权限 | 有多维画像查询权限的用户可发起查询 |
| 导出权限 | 有日志导出权限的用户可导出详情；无权限时不展示或禁用导出入口 |

**通用状态及交互：**

| 场景 | 说明 |
| --- | --- |
| 默认 tab | 日志信息默认选中“登录” |
| 分页 | 默认 5 条/页，支持切换 10/20 条/页 |
| 空状态 | 日志和关联信息无数据时展示统一空状态，文案“暂无数据” |
| 复制 | 标识类字段支持复制，点击后 toast 提示“xxx复制成功” |
| 导出 | 导出 Excel 文件，包含所有日志 tab；Excel 内通过 sheet 区分 tab，每列为独立字段 |
| 关闭 | 点击右上角关闭按钮或按 Esc 关闭弹窗 |
| 底部按钮 | 本期不展示弹窗底部按钮 |

**2.4 账号画像查询弹窗**

账号维度查询成功后，打开“账号画像查询”弹窗。

**基础信息字段：**

| 区域 | 字段 | 展示说明 |
| --- | --- | --- |
| 顶部固定字段 | 账号名、通行证ID、SDKID | 默认展示，固定三个字段 |
| 账号绑定信息 | 登录手机、安全手机、安全邮箱、实名认证 | 展开账号信息后展示 |
| 最近登录信息 | 登录时间、登录IP、登录设备ID、登录方式 | 展开账号信息后展示 |
| 账号注册信息 | 注册时间、注册IP、注册设备ID、注册方式 | 展开账号信息后展示 |

**状态及交互：**

| 场景 | 说明 |
| --- | --- |
| 默认状态 | 账号信息默认收起，仅展示顶部固定字段 |
| 展开账号信息 | 点击右侧箭头后展示账号绑定信息、最近登录信息、账号注册信息 |
| 收起账号信息 | 再次点击箭头后收起详情，仅保留顶部固定字段 |

**日志 tab：**

| tab | 是否展示 | 说明 |
| --- | --- | --- |
| 登录 | 是 | 默认选中 |
| 注册 | 否 | 账号维度本期不展示 |
| 支付 | 是 | 支持切换查看 |

**关联信息：**

| 关联对象 | 展示内容 | 第二行展示 | 排序 |
| --- | --- | --- | --- |
| 关联设备 | 设备ID | 设备型号 | 按关联次数倒序 |
| 关联IP | IP地址 | 归属地 | 按关联次数倒序 |

**2.5 设备画像查询弹窗**

设备维度查询成功后，打开“设备画像查询”弹窗。

**基础信息字段：**

| 区域 | 字段 | 展示说明 |
| --- | --- | --- |
| 顶部固定字段 | 设备ID、设备指纹 | 默认展示 |
| 设备详情 | 设备品牌、系统类型、设备型号、操作系统版本、设备语言、是否为模拟器 | 默认展开展示 |

**日志 tab：**

| tab | 是否展示 | 说明 |
| --- | --- | --- |
| 登录 | 是 | 默认选中 |
| 注册 | 是 | 支持切换查看 |
| 支付 | 是 | 支持切换查看 |

**关联信息：**

| 关联对象 | 展示内容 | 第二行展示 | 排序 |
| --- | --- | --- | --- |
| 关联账号 | SDKID | 账号名 | 按关联次数倒序 |
| 关联IP | IP地址 | 归属地 | 按关联次数倒序 |

**2.6 IP画像查询弹窗**

IP 维度查询成功后，打开“IP画像查询”弹窗。

**基础信息字段：**

| 区域 | 字段 | 展示说明 |
| --- | --- | --- |
| 顶部固定字段 | IP地址 | 默认展示 |
| IP详情 | IP归属地、网络类型、网络运营商 | 默认展开展示 |

**日志 tab：**

| tab | 是否展示 | 说明 |
| --- | --- | --- |
| 登录 | 是 | 默认选中 |
| 注册 | 是 | 支持切换查看 |
| 支付 | 是 | 支持切换查看 |

**关联信息：**

| 关联对象 | 展示内容 | 第二行展示 | 排序 |
| --- | --- | --- | --- |
| 关联账号 | SDKID | 账号名 | 按关联次数倒序 |
| 关联设备 | 设备ID | 设备型号 | 按关联次数倒序 |

**2.7 标签信息**

**字段/展示规则：**

| 项目 | 说明 |
| --- | --- |
| 标签内容 | 展示当前查询对象命中的标签 |
| 默认展示 | 默认最多展示 1 行 |
| 超出一行 | 右侧展示“查看更多 >” |
| 点击查看更多 | 展开当前标签区域，展示全部标签 |
| 删除能力 | 不支持移除，不展示删除图标 |
| 颜色规则 | 仅展示标签，不按阈值区分颜色 |

**2.8 日志信息**

日志信息展示当前查询对象近 30 日登录、注册、支付相关行为。账号维度仅展示登录、支付；设备和 IP 维度展示登录、注册、支付。

**登录日志字段：**

| 字段 | 展示说明 |
| --- | --- |
| 登录时间 | 登录行为发生时间 |
| 账号信息 | 账号标识及账号名；当前查询对象为账号时不重复展示账号列 |
| 设备信息 | 设备ID及设备型号；当前查询对象为设备时不重复展示设备列 |
| IP信息 | IP地址及归属地；当前查询对象为 IP 时不重复展示 IP 列 |
| 风险分 | 仅展示风险分值 |
| 处置动作 | 展示处置动作枚举值 |

**注册日志字段：**

| 字段 | 展示说明 |
| --- | --- |
| 注册时间 | 注册行为发生时间 |
| 账号信息 | 账号标识及账号名 |
| 设备信息 | 设备ID及设备型号 |
| IP信息 | IP地址及归属地 |
| 风险分 | 仅展示风险分值 |
| 处置动作 | 展示处置动作枚举值 |

**支付日志字段：**

| 字段 | 展示说明 |
| --- | --- |
| 下单时间 | 支付/下单行为发生时间 |
| 下单信息 | 订单号及金额 |
| 账号信息 | 账号标识及账号名 |
| 设备信息 | 设备ID及设备型号 |
| IP信息 | IP地址及归属地 |
| 风险分 | 仅展示风险分值 |
| 处置动作 | 展示处置动作枚举值 |

**状态及交互：**

| 场景 | 说明 |
| --- | --- |
| 时间范围 | 固定展示近 30 日数据，不支持筛选 |
| 默认分页 | 默认 5 条/页 |
| 分页切换 | 支持 10 条/页、20 条/页 |
| 风险分颜色 | 不按阈值区分颜色 |
| 空状态 | 无数据时展示“暂无数据” |
| 导出 | 导出所有日志 tab 数据，不仅导出当前 tab |

**处置动作枚举：**

| 枚举值 |
| --- |
| 验证码验证 |
| 验证码验证通过 |
| 滑块验证 |
| 滑块验证通过 |
| 放行 |
| 拦截 |

**2.9 复制、导出与反馈**

**复制规则：**

| 项目 | 说明 |
| --- | --- |
| 支持复制字段 | 账号名、通行证ID、SDKID、手机号、邮箱、设备ID、设备指纹、IP地址、订单号等标识类字段 |
| 复制反馈 | 点击复制后 toast 提示“xxx复制成功”，xxx 为字段名称 |
| 关联对象 | 不支持点击跳转查询，仅支持复制 ID 后手动查询 |

**导出规则：**

| 项目 | 说明 |
| --- | --- |
| 导出入口 | 日志信息模块右侧“导出详情” |
| 导出范围 | 当前查询对象的全部日志 tab 数据 |
| 文件格式 | Excel |
| Sheet 规则 | Excel 内通过 sheet 区分登录、注册、支付 |
| 字段规则 | 每列为一个独立字段 |
| 成功反馈 | 生成文件后 toast 提示导出成功 |
| 失败反馈 | toast 提示“导出失败，请稍后重试” |

**2.10 异常处理**

| 场景 | 展示位置 | 文案 | 处理规则 |
| --- | --- | --- | --- |
| 查询内容为空 | 输入框下方、toast | 请输入{当前字段名} | 阻断查询，不打开弹窗 |
| 查询对象不存在 | toast | 未查询到相关画像信息，请检查输入内容 | 保留输入，允许用户修改后重试 |
| 查询接口失败 | toast | 查询失败，请稍后重试 | 保留输入，允许重试 |
| 查询超时 | toast | 查询超时，请稍后重试 | 保留输入，允许重试 |
| 无查询权限 | toast | 暂无权限查看该画像信息 | 阻断查询或隐藏入口，按权限配置处理 |
| 导出失败 | toast | 导出失败，请稍后重试 | 保留当前弹窗，不影响查询结果查看 |

# 附录

## 1. 设计稿映射

| 序号 | 内容 | 链接 | 备注 |
| --- | --- | --- | --- |
| 1 | 多维画像查询基础页-按账号 | [查看设计稿](https://www.figma.com/design/9JqT9uhKrNIwOgnYZQL1ox/%E9%A3%8E%E6%8E%A7%E7%B3%BB%E7%BB%9F?node-id=2470-6652) | 账号维度查询入口 |
| 2 | 多维画像查询基础页-按设备 | [查看设计稿](https://www.figma.com/design/9JqT9uhKrNIwOgnYZQL1ox/%E9%A3%8E%E6%8E%A7%E7%B3%BB%E7%BB%9F?node-id=2470-8210) | 设备维度查询入口 |
| 3 | 多维画像查询基础页-按IP | [查看设计稿](https://www.figma.com/design/9JqT9uhKrNIwOgnYZQL1ox/%E9%A3%8E%E6%8E%A7%E7%B3%BB%E7%BB%9F?node-id=2470-8898) | IP 维度查询入口 |
| 4 | 账号查询结果-展开态/登录日志 | [查看设计稿](https://www.figma.com/design/9JqT9uhKrNIwOgnYZQL1ox/%E9%A3%8E%E6%8E%A7%E7%B3%BB%E7%BB%9F?node-id=2483-18710) | 账号弹窗展开示例 |
| 5 | 账号查询结果-收起态/支付日志 | [查看设计稿](https://www.figma.com/design/9JqT9uhKrNIwOgnYZQL1ox/%E9%A3%8E%E6%8E%A7%E7%B3%BB%E7%BB%9F?node-id=2485-19892) | 账号弹窗收起示例 |
| 6 | 账号查询结果-空数据 | [查看设计稿](https://www.figma.com/design/9JqT9uhKrNIwOgnYZQL1ox/%E9%A3%8E%E6%8E%A7%E7%B3%BB%E7%BB%9F?node-id=2486-21204) | 空状态示例，三类维度复用 |
| 7 | 设备画像查询弹窗 | [查看设计稿](https://www.figma.com/design/9JqT9uhKrNIwOgnYZQL1ox/%E9%A3%8E%E6%8E%A7%E7%B3%BB%E7%BB%9F?node-id=2489-23400) | 设备维度结果弹窗 |
| 8 | IP画像查询弹窗 | [查看设计稿](https://www.figma.com/design/9JqT9uhKrNIwOgnYZQL1ox/%E9%A3%8E%E6%8E%A7%E7%B3%BB%E7%BB%9F?node-id=2490-26505) | IP 维度结果弹窗 |

## 2. 待确认项

| 问题 | 当前处理 |
| --- | --- |
| 查询接口字段口径 | 后续对接查询结果服务时确认 |
| 最近查询记录存储方式 | 后续确认由前端本地存储、用户偏好服务或后端最近查询接口承接 |
| 真实导出文件格式 | 当前需求定义为 Excel；后续由技术确认采用 xls、xlsx 或服务端导出 |
