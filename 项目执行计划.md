# 风控系统多维画像查询页面 - 项目执行计划

## 1. 当前结论

本项目第一阶段建议做成本地可点击高保真原型，优先还原 Figma 已有的「多维画像查询」入口页，并补齐查询后结果页/详情区的体验闭环。当前仓库仅有 `AGENTS.md`，尚未创建前端工程，因此执行顺序应为：设计确认 -> 原型工程初始化 -> 首页查询闭环 -> 画像结果模块 -> 浏览器视觉验收。

## 2. 已知输入

| 项目 | 内容 |
| --- | --- |
| 项目目标 | 实现风控系统的多维画像查询页面 |
| 设计稿 | Figma「风控系统」节点 `2470:6307`，页面名「画像查询」 |
| 已识别页面 | 「用户登录查询-按账号查询」主画面，包含按账号/按设备/按 IP 三种查询维度 |
| 已识别导航 | 平台安全中心 / 用户画像中心 / 多维画像查询 |
| 已识别入口模块 | 页头说明、查询卡片、最近查询 |
| 本地状态 | `D:\proj\画像查询` 目前只有项目说明文件，未发现前端代码或依赖 |

## 3. 第一阶段范围

| 范围 | 包含 |
| --- | --- |
| 页面框架 | 顶部导航、左侧菜单、内容区标题、背景与卡片层级 |
| 查询入口 | 按账号、按设备、按 IP 三个 tab；字段类型下拉；输入框；立即查询按钮 |
| 最近查询 | 历史标签展示、点击回填、单项删除、清空记录 |
| 查询反馈 | loading、空输入校验、无结果、查询失败、正常结果 |
| 查询结果 | 风险概览、基础画像、关联账号/设备/IP、风险事件、登录/注册/支付场景记录 |
| 原型数据 | 本地 mock 数据，不接后端、不做登录、不做权限系统 |
| 验收 | 构建/运行检查、浏览器主流程点击、对照 Figma 截图做视觉自检 |

## 4. 第一阶段暂不包含

| 暂不包含 | 原因 |
| --- | --- |
| 后端接口与数据库 | 当前定位为 PM 本地高保真原型，优先评审体验 |
| 登录鉴权与真实用户体系 | 设计稿中只作为系统外壳展示，不影响画像查询主流程 |
| 复杂图谱引擎 | 可先用静态关系图/表格表达，避免过早引入重依赖 |
| 导出、批量查询、任务队列 | 属于二期增强，不影响第一阶段闭环 |
| 生产部署流程 | 本地运行即可 |

## 5. 信息架构

| 层级 | 模块 | 说明 |
| --- | --- | --- |
| 系统外壳 | Header + Sidebar | 还原安全中心风格，突出「用户画像中心 / 多维画像查询」当前位置 |
| 查询入口 | QueryPanel | 三维度 tab，根据维度切换字段、placeholder、辅助说明 |
| 历史入口 | RecentQueries | 展示历史查询对象，支持回填与清空 |
| 结果总览 | RiskSummary | 展示对象身份、风险等级、命中标签、最近活跃、关联数量 |
| 画像详情 | ProfileSections | 基础信息、账号行为、设备/IP 关联、风险标签 |
| 风险证据 | EvidencePanel | 登录、注册、支付事件列表，支持按场景切换 |
| 边界状态 | Empty/Error/Loading | 保证评审时能看到完整状态，不只做理想态 |

## 6. 推荐页面流程

| 步骤 | 用户动作 | 页面响应 |
| --- | --- | --- |
| 1 | 进入多维画像查询页 | 默认展示「按账号」，显示最近查询 |
| 2 | 切换按设备/按 IP | 查询字段、输入提示、说明文案同步切换 |
| 3 | 输入对象并点击立即查询 | 按钮进入 loading，随后展示结果区 |
| 4 | 查看风险概览 | 顶部给出风险等级、关键标签、关联对象数量 |
| 5 | 切换证据场景 | 登录/注册/支付事件列表切换 |
| 6 | 点击历史查询标签 | 回填查询条件并刷新结果 |
| 7 | 删除/清空历史 | 标签即时移除，空历史时展示空状态 |

## 7. 实施计划

| 阶段 | files | action | verify | done |
| --- | --- | --- | --- | --- |
| P0 设计读取与确认 | Figma 节点、截图素材 | 继续读取按账号/按设备/按 IP 关键 frame；确认是否还有结果页设计稿 | 记录节点、截图、模块清单 | 明确首页与结果页是否都按 Figma 还原 |
| P1 工程初始化 | `package.json`、`src/`、`index.html` 等 | 创建轻量前端原型工程，优先 React + Vite + Ant Design 或纯 HTML 方案二选一 | `npm install` 后运行 dev server | 本地能打开空壳页面 |
| P2 系统外壳 | `src/App.*`、layout 样式 | 实现 Header、Sidebar、内容容器，匹配设计稿密度与色彩 | 浏览器截图检查 1920 宽布局 | 页面外壳与 Figma 首屏一致 |
| P3 查询入口 | `QueryPanel`、query state | 实现三维度 tab、字段下拉、输入、查询校验、loading | 点击三种 tab 和查询按钮 | 查询入口完整可用 |
| P4 最近查询 | `RecentQueries`、mock history | 实现历史标签、回填、删除、清空、空状态 | 点击回填/删除/清空 | 最近查询形成闭环 |
| P5 画像结果 | `RiskSummary`、`ProfileSections`、mock data | 查询后展示对象画像、风险标签、关联对象、证据列表 | 用账号/设备/IP 三类 mock 查询 | 三种维度都有可信结果 |
| P6 边界状态 | `EmptyState`、`ErrorState`、`LoadingState` | 增加空输入、无结果、失败、长文本、多标签换行等状态 | 手动触发每种状态 | 评审时不会只看到理想态 |
| P7 视觉 QA | 浏览器截图、Figma 截图 | 对照 Figma 检查模块顺序、间距、字体、颜色、遮挡和换行 | 桌面视口主流程截图 | 页面达到可评审标准 |

## 8. Mock 数据设计

| 数据对象 | 字段建议 |
| --- | --- |
| 查询对象 | `type`、`keyword`、`displayName`、`id`、`riskLevel`、`lastActiveAt` |
| 风险标签 | `label`、`level`、`source`、`hitCount`、`updatedAt` |
| 关联对象 | `accounts`、`devices`、`ips`、`phones`、`sdkIds` |
| 场景事件 | `scene`、`time`、`result`、`riskReason`、`ip`、`device`、`location` |
| 最近查询 | `type`、`keyword`、`createdAt` |

## 9. 视觉验收标准

| 检查项 | 标准 |
| --- | --- |
| 布局 | Header 60px、Sidebar 约 200px、内容区卡片顺序与 Figma 一致 |
| 文案 | 标题「多维画像查询」，说明「支持账号、设备、IP维度的用户洞察，快速定位登录、注册、支付风险」 |
| 查询区 | tab 选中态、输入框、按钮、辅助说明清晰；三种维度切换无跳动 |
| 历史区 | 标签可读，过多时自然换行，不遮挡、不溢出 |
| 结果区 | 查询前不喧宾夺主，查询后主次分明，风险信息优先展示 |
| 边界态 | loading、empty、error 不破坏布局 |
| 可点击 | entry -> query -> result -> history retry -> clear/back 流程可走通 |

## 10. 风险与待确认

| 问题 | 影响 | 建议 |
| --- | --- | --- |
| 是否已有结果页 Figma 设计 | 决定结果区是严格还原还是基于产品逻辑补设计 | P0 阶段继续检索 Figma 节点，若无则先做 PM 可评审版本 |
| 技术栈未初始化 | 影响后续文件路径和验证命令 | 若只做本地原型，建议 React + Vite + Ant Design；若要单文件交付，可改为纯 HTML |
| 真实字段口径未确认 | 影响画像结果可信度 | 第一阶段用 mock，但字段命名保持风控业务语义 |
| 关系图复杂度 | 可能拖慢原型进度 | 第一阶段用卡片/表格表达关联，二期再考虑图谱 |

## 11. 下一步建议

| 优先级 | 动作 |
| --- | --- |
| 1 | 确认第一阶段技术形态：React + Vite + Ant Design，或单 HTML 原型 |
| 2 | 继续读取 Figma 中按设备、按 IP、可能存在的结果页节点 |
| 3 | 初始化工程并实现 Figma 首屏查询页 |
| 4 | 补齐 mock 结果页和查询闭环 |
| 5 | 运行本地页面，做浏览器级视觉自检并修正 |
